export interface Shedules {
  id: number;
  date: Date;
  startTime: string;
  endTime: string;
  name: string;
}

export interface VectorIconProps {
  size?: number;
  color?: string;
  name?: string;
}
